# InkTime

墨水屏电子相框系统：树莓派渲染每日照片，ESP32 相框设备定时唤醒拉取并显示，舵机按照片朝向翻转相框。

## Language

### 设备生命周期

**主设备**：
运行 `esp32/ink-display-wifi-epd` 固件的相框本体（墨水屏 + 舵机 + ESP32）。
_Avoid_: 显示端、客户端

**定时唤醒**：
设备按刷新计划从深度睡眠自动醒来，唤醒即一次完整重启，醒来后直接执行"同步时间 → 拉取照片 → 转舵机 → 刷屏 → 再睡"。

**手动唤醒**：
按复位键/上电导致的启动（非定时唤醒）。会打开 3 分钟局域网 Web 管理窗口。

**标定**：
为相框记录竖屏/横屏两个舵机目标角度并写入设备的过程。未标定时舵机被禁用、不参与流程。
_Avoid_: 校准、调零

**朝向（Orientation）**：
一张照片/相框当前应处的姿势：`portrait`（竖屏）或 `landscape`（横屏）。决定舵机目标角度与画面旋转。

### 设备日志

**设备日志（Device Journal）**：
设备端记录的追加式关键事件流水，存于设备 Flash，联网时集中上传到树莓派，作为跨重启的事故回查依据（"黑盒"）。
_Avoid_: 调试日志、串口日志（串口输出不属于设备日志）

**事件（Event）**：
设备日志中的一条原子记录：开机序号 + 相对毫秒 + 事件类型 + 少量键值。

**开机序号（Boot Seq）**：
设备每次开机递增 1 的持久计数，用于给事件排序分组——同一序号的事件属于同一次唤醒。

**锚点（Anchor）**：
时间同步成功后记录的一条特殊事件，携带当时的真实墙钟时间；树莓派据此把所有事件的相对毫秒换算为绝对时间。

**上传游标（Upload Cursor）**：
标记已成功上传到树莓派的事件位置；只有收到服务器 200 才推进，之前的事件保留在 Flash 重传。
